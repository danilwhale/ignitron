<Project>

    <Import Project="$(MSBuildThisFileDirectory)helpers\Ignitron.Helpers.ValidateGameDirectory.targets"/>

    <ItemGroup>
        <!-- obvious reference -->
        <Reference Include="$([System.IO.Path]::Combine('$(GameDirectory)', 'Allumeria.dll'))" Private="false"/>

        <!-- game dependencies -->
        <Reference Include="$([System.IO.Path]::Join('$(GameDirectory)', 'FastNoise.dll'))" Private="false"/>
        <Reference Include="$([System.IO.Path]::Join('$(GameDirectory)', 'OpenTK.Audio.OpenAL.dll'))" Private="false"/>
        <Reference Include="$([System.IO.Path]::Join('$(GameDirectory)', 'OpenTK.Compute.dll'))" Private="false"/>
        <Reference Include="$([System.IO.Path]::Join('$(GameDirectory)', 'OpenTK.Core.dll'))" Private="false"/>
        <Reference Include="$([System.IO.Path]::Join('$(GameDirectory)', 'OpenTK.Graphics.dll'))" Private="false"/>
        <Reference Include="$([System.IO.Path]::Join('$(GameDirectory)', 'OpenTK.Input.dll'))" Private="false"/>
        <Reference Include="$([System.IO.Path]::Join('$(GameDirectory)', 'OpenTK.Mathematics.dll'))" Private="false"/>
        <Reference Include="$([System.IO.Path]::Join('$(GameDirectory)', 'OpenTK.Windowing.Common.dll'))" Private="false"/>
        <Reference Include="$([System.IO.Path]::Join('$(GameDirectory)', 'OpenTK.Windowing.Desktop.dll'))" Private="false"/>
        <Reference Include="$([System.IO.Path]::Join('$(GameDirectory)', 'OpenTK.Windowing.GraphicsLibraryFramework.dll'))" Private="false"/>
        <Reference Include="$([System.IO.Path]::Join('$(GameDirectory)', 'SoLoudLib.dll'))" Private="false"/>
        <Reference Include="$([System.IO.Path]::Join('$(GameDirectory)', 'SQLitePCLRaw.batteries_v2.dll'))" Private="false"/>
        <Reference Include="$([System.IO.Path]::Join('$(GameDirectory)', 'SQLitePCLRaw.core.dll'))" Private="false"/>
        <Reference Include="$([System.IO.Path]::Join('$(GameDirectory)', 'SQLitePCLRaw.provider.e_sqlite3.dll'))" Private="false"/>
        <Reference Include="$([System.IO.Path]::Join('$(GameDirectory)', 'StbImageSharp.dll'))" Private="false"/>
        <Reference Include="$([System.IO.Path]::Join('$(GameDirectory)', 'StbImageWriteSharp.dll'))" Private="false"/>
    </ItemGroup>

</Project>